# Core Django packages (REQUIRED)
Django==5.2
djangorestframework==3.16.0
djangorestframework-simplejwt==5.3.0
django-cors-headers==4.7.0

# Python packaging tools (REQUIRED for pkg_resources)
setuptools==75.0.0
wheel==0.44.0
packaging==25.0

# Database (REQUIRED)
psycopg2-binary==2.9.9
dj-database-url==2.1.0

# Authentication & Security (REQUIRED)
python-decouple==3.8

# Static files (REQUIRED)
whitenoise==6.6.0

# Excel handling (REQUIRED for your app)
openpyxl==3.1.2

# Email & OTP (REQUIRED for your features)
pyotp==2.9.0
qrcode==7.4.2

# Core Python dependencies
asgiref==3.8.1
python-dateutil==2.9.0.post0
pytz==2025.2
sqlparse==0.5.3
tzdata==2025.2
six==1.17.0

# Production server (if needed)
gunicorn==21.2.0

# Image processing (if you use image uploads)
Pillow==10.1.0