# Streamlined requirements for Vercel deployment
# Core Django packages
asgiref==3.8.1
Django==5.2
django-cors-headers==4.7.0
djangorestframework==3.16.0
djangorestframework-simplejwt==5.3.0

# Database
psycopg2-binary==2.9.9
dj-database-url==2.1.0

# Utilities
python-decouple==3.8
python-dateutil==2.9.0.post0
pytz==2025.2
six==1.17.0
sqlparse==0.5.3
tzdata==2025.2

# Static files and deployment
whitenoise==6.6.0
gunicorn==21.2.0

# Excel handling (essential for HRMS)
openpyxl==3.1.2

# Authentication and security
pyotp==2.9.0
qrcode==7.4.2

# Email functionality
google-auth==2.23.4
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.2.0
google-api-python-client==2.108.0

# Optional: Remove if not essential for core functionality
# numpy==2.2.4  # Heavy package - only include if absolutely necessary
# pandas==2.2.3  # Heavy package - only include if absolutely necessary
# Pillow==10.1.0  # Only if image processing is needed
# boto3==1.34.0  # Only if AWS S3 is used
# django-storages==1.14.2  # Only if cloud storage is used
# celery==5.3.4  # Not supported on Vercel serverless
# redis==5.0.1  # Not supported on Vercel serverless
# xlrd==2.0.1  # Only if reading old Excel formats